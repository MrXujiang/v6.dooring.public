import React from 'react'

interface LoadingCpProps {
  style?: React.CSSProperties | undefined
}

export default ({ style }: LoadingCpProps) => <div style={style} className="rotate-animate">Dooring</div>
